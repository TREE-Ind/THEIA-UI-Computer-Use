# Heavy visual-grounding dependencies for THEIA's isolated LocateAnything worker.
# These are installed by scripts/setup_locate_worker.py into a worker venv, not
# into the live Hermes Agent runtime environment.
-r requirements-basic.txt
opencv-python>=4.8.0
transformers==4.57.1
peft>=0.13.0
accelerate>=0.33.0
sentencepiece>=0.2.0
lmdb>=1.5.0
decord>=0.6.0
hf_xet>=1.1.0
# Torch/torchvision are selected separately by setup_locate_worker.py via
# THEIA_LOCATE_TORCH or --torch: auto, cpu, default, cu121, cu124, cu126, skip.
